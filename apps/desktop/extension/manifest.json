{
  "manifest_version": 3,
  "name": "WorkCrew Browser Connector",
  "version": "1.0.0",
  "description": "Lets the WorkCrew app work in the browser you already use, with the accounts you are already signed into.",
  "permissions": ["tabs", "scripting", "storage", "alarms"],
  "host_permissions": ["http://*/*", "https://*/*"],
  "background": { "service_worker": "background.js" },
  "options_page": "options.html",
  "action": { "default_popup": "options.html", "default_title": "WorkCrew" }
}
